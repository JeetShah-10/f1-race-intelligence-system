# Environment files (NEVER commit these!)
.env
.env.local
.env.*.local
*.env

# Agent-specific (private)
.agent/
.antigravity/
.context/

# Reference files (too large for regular git)
3D-model-and-references/
ml_data/
backend/cache/

# Python
venv/
__pycache__/
*.pyc
*.pyo

# Node
node_modules/
dist/
build/

# IDE
.vscode/
.idea/
*.swp
*.swo

# Agent/Tool configs
.clauderc.json
.gemini/

# OS
Thumbs.db

# Large Models & DBs
*.db
backend/f1_sim.db
*.pkl
!backend/app/models/*.pkl # Keep small models if needed, but risky. Better to ignore all large ones.
# Actually, let's ignore all pkl for now to be safe, or just large ones.
# user's pkls were small (1.6MB), so maybe keep them?
# The error was 560MB file. That was likely in backend/cache.

# Caches
cache/
backend/cache/

# Logs & debug output
*.log
*_output.txt

# Temp tracking files
tracked_files.txt
git_status.txt

# Legacy directories (removed from project)
F1_Intelligence_Model/
f1-race-prediction/
3D-model-backup/
notebooks/
ml_notebooks/
